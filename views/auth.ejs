<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Skill-Swapz - Sign Up & Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css">
    <style>
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #0f2027, #2c5364 80%);
            color: #fff;
        }
        .auth-card {
            background: rgba(255,255,255,0.07);
            border-radius: 20px;
            box-shadow: 0 8px 32px 0 rgba(31,38,135,0.37);
            backdrop-filter: blur(4px);
            -webkit-backdrop-filter: blur(4px);
            border: 1px solid rgba(255,255,255,0.18);
            padding: 2.5rem 2.5rem 2rem 2.5rem;
            margin-top: 3rem;
            max-width: 400px;
        }
        .auth-card h2 {
            font-weight: bold;
            color: #ffd700;
        }
        .form-control, .btn {
            border-radius: 10px;
        }
        .btn-custom {
            background: #ffd700;
            color: #222;
            font-weight: bold;
            border: none;
        }
        .btn-custom:hover {
            background: #fff;
            color: #222;
        }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center" style="min-height:100vh;">
        <div class="auth-card text-center">
            <h2>Sign Up</h2>
            <form action="/signup" method="POST" class="mb-4">
                <input type="text" name="username" class="form-control mb-3" placeholder="Username" required>
                <input type="password" name="password" class="form-control mb-3" placeholder="Password" required>
                <button type="submit" class="btn btn-custom w-100 mb-2">Sign Up</button>
            </form>
            <hr class="my-4" style="border-color: #ffd700;">
            <h2>Login</h2>
            <form action="/login" method="POST">
                <input type="text" name="username" class="form-control mb-3" placeholder="Username" required>
                <input type="password" name="password" class="form-control mb-3" placeholder="Password" required>
                <button type="submit" class="btn btn-custom w-100">Login</button>
            </form>
            <a href="/" class="btn btn-outline-light mt-4 w-100">Back to Home</a>
        </div>
    </div>
</body>
</html>